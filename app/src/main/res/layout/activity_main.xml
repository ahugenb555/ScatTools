<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/scat_bar_height"
        android:gravity="center_horizontal"
        android:orientation="horizontal"
        android:paddingTop="@dimen/scat_padding_small"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/scat_btn_die"
            android:layout_width="match_parent"
            android:layout_height="@dimen/scat_btn_height"
            android:layout_marginEnd="@dimen/scat_padding_small"
            android:layout_weight="1"
            android:textSize="@dimen/scat_text_small"
            android:text="tap to roll"
            android:backgroundTint="@color/black">
        </com.google.android.material.button.MaterialButton>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/scat_btn_timer"
            android:layout_width="match_parent"
            android:layout_height="@dimen/scat_btn_height"
            android:layout_weight="1"
            android:backgroundTint="@color/black"

            android:paddingStart="@dimen/scat_padding_small"></com.google.android.material.button.MaterialButton>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/scat_btn_reset"
            android:layout_width="match_parent"
            android:layout_height="@dimen/scat_btn_height"
            android:layout_weight="1"

            android:backgroundTint="@color/black"></com.google.android.material.button.MaterialButton>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>